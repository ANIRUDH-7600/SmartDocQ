# Flask service environment
PORT=5001

# CORS: allowed frontend origins (comma-separated). Wildcards supported with leading '*.' (e.g., *.vercel.app)
FRONTEND_ORIGINS=http://localhost:3000, https://smartdocq.vercel.app
# Optional: include previews with wildcard
# Node API base URL for server-to-server document fetch
NODE_BASE_URL=http://localhost:5000
# Service token that must match servers/.env SERVICE_TOKEN
# Use a strong random secret (do not use a well-known default)
SERVICE_TOKEN=

# Gemini configuration
GEMINI_API_KEY=
TEXT_MODEL=models/gemini-2.5-flash
EMBED_MODEL=models/text-embedding-004
